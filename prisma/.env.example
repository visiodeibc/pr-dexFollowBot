# Copy to prisma/.env and fill values from your Supabase dashboard

# Pooled connection (app runtime). Use Connection Pooling (pgBouncer) host.
# Get the exact host/port from: Supabase → Project Settings → Database → Connection string → Pooler
# Example format shown below; replace placeholders with your real values.
DATABASE_URL="postgresql://postgres:<DB_PASSWORD>@<POOLER_HOST>:6543/postgres?pgbouncer=true&sslmode=require"

# Direct connection (Prisma migrations/introspection). Use the Direct host (db.<project>.supabase.co).
# Get it from: Supabase → Project Settings → Database → Connection string → Direct (psql)
DIRECT_URL="postgresql://postgres:<DB_PASSWORD>@db.<PROJECT_REF>.supabase.co:5432/postgres?sslmode=require"

# Optional: Local shadow DB for `prisma migrate dev` (only if you run dev migrations locally)
# Start local Postgres then create an empty DB named prisma_shadow
# docker run --name pg -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres:16
# createdb -h localhost -U postgres prisma_shadow
#SHADOW_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/prisma_shadow"

